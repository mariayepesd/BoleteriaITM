{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

/*

  {
  "Logging": { // Configuraci+on standar de logging para ASP.NET Core
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*", //  Configuracion standar de red 

  // Configuración de YARP para el gateway
  "ReverseProxy": { // Sección principal de configuración de YARP
    //1. RUTAS (Lo que el Gateway escucha)
    "Routes": { // Sección de rutas, cada ruta define cómo se redirige el tráfico
      "inventory-route": {
        "ClusterId": "inventory-cluster",
        "Match": {
          "Path": "/bodega/{**catch-all}" // Si la url empieza con /bodega, se redirige a inventory-cluster
        },

        "Transforms": [
          {
            "PathPattern": "/api/inventory/{**catch-all}" // Lo traduce a una ruta real del microservicio

        ]
      }
    },
    //2. CLUSTER (Los servidores reales ocultos. A dónde se redirige)

    "Clusters": { // Sección de clusters, cada cluster define un grupo de destinos a los que se puede redirigir el tráfico
      "inventory-cluster": {
        "Destinations": {
          // IMPORTANTE:  El puerto debe coincidir con el puerto del microservicio de inventario Itm.Inventory.Api
          "destination1": {
            "Address": "http://localhost:5273/"
          }
        }
      }
    }
  }
}



*/
